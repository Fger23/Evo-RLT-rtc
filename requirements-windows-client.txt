# Native Windows REMOTE recording client, using this exact repository checkout.
# New environments: use 64-bit Python 3.11 (or match the working server's Python).
# Install a matching CPU PyTorch/torchvision pair first. Example for torch 2.7.1:
# python -m pip install torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cpu
# If the Ubuntu server uses another supported torch version, match that version
# and its corresponding torchvision release instead of changing a working server.
# Then:
# python -m pip install -r requirements-windows-client.txt
# python -m pip install -e . --no-deps
# --no-deps is intentional: pyproject's complete training/robot environment also
# resolves Piper/Pinocchio and other packages not needed by the SO/Feetech client.
# This file supplies the recording runtime; --no-deps alone does NOT set it up.
# Existing working RTC environments: preserve torch, camera and motor SDK versions;
# install only missing/incompatible requirements rather than upgrading everything.
# No VLA weights, CUDA, transformers, diffusers, torchcodec, Piper or Pinocchio are
# needed for remote_policy.enable=true. This is NOT a server/training requirements file.

torch>=2.6.0,<2.8.0
torchvision>=0.21.0,<0.23.0
numpy>=1.26.0,<3.0.0
datasets>=4.0.0,<4.2.0
huggingface-hub[hf-transfer,cli]>=0.34.2,<0.36.0
accelerate>=1.10.0,<2.0.0
einops>=0.8.0,<0.9.0
opencv-python-headless>=4.9.0,<4.13.0
av>=15.0.0,<16.0.0
jsonlines>=4.0.0,<5.0.0
packaging>=24.2,<26.0
pynput>=1.7.7,<1.9.0
pyserial>=3.5,<4.0
draccus==0.10.0
gymnasium>=1.1.1,<2.0.0
rerun-sdk>=0.24.0,<0.27.0
deepdiff>=7.0.1,<9.0.0
imageio[ffmpeg]>=2.34.0,<3.0.0
termcolor>=2.4.0,<4.0.0
safetensors>=0.4.3,<1.0.0

# Wire protocol generated by this checkout. Install the same versions on both hosts.
grpcio==1.73.1
protobuf>=6.31.1,<6.32.0

# SO100/SO101/bimanual SO hardware. Omit if a different supported robot is used.
# PyPI 1.0.0 publishes an sdist; normal pip builds its small py3-none-any wheel.
# Therefore do not use --only-binary=:all: for the normal installation command.
feetech-servo-sdk>=1.0.0,<2.0.0

# Optional Intel RealSense camera (omit this command when all cameras are OpenCV):
# python -m pip install "pyrealsense2>=2.55.1.6486,<2.57.0"
# Do not install opencv-python and opencv-python-headless into the same environment.
# A working environment which already uses opencv-python can keep that provider;
# both expose cv2. The recorder's display_data uses Rerun, not cv2.imshow.
